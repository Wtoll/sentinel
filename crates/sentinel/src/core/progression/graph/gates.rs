//! Key-Lock style progress gates
//! 

use bevy::prelude::*;

// use crate::core::progression::graph::nodes_behind;

// /// A lock that is opened by a key
// #[derive(Component)]
// #[relationship(relationship_target = Key)]
// pub struct Lock(pub Entity);

// /// A key that opens locks
// #[derive(Component, Default)]
// #[relationship_target(relationship = Lock)]
// pub struct Key(Vec<Entity>);






// fn create_key_lock(commands: &mut Commands, node: Entity) {

//     let key = commands.spawn(Key::default()).id();

//     let lock = commands.spawn(Lock(key)).id();






// }


